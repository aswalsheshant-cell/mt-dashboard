# Failure-Mode Register

Created 2026-09-13. This catalogues failure PATTERNS found in this project so far,
each with its preventive control, not just its one-off fix — so the same class of
defect is caught before it reaches the dashboard next time, rather than rediscovered
from scratch in a future session. Add a new `FM-NN` row whenever a material defect
is found; do not delete a row once a pattern has occurred, even after the specific
instance is fixed — the control is what stays permanent.

Every row below is a REAL incident from this repository's own history (not a
hypothetical), each already fixed at least once. Preventive controls marked
`NOT YET BUILT` are honestly flagged as such, not claimed as done.

| Failure_ID | Category | Description | Root Cause | Detection | Preventive Control | Regression Test | Severity | Status |
|---|---|---|---|---|---|---|---|---|
| FM-01 | DATA_QUALITY | Seed template's own "EXAMPLE ROW" placeholder rows silently entered production CM2 as if real Finance expense | `load_pl_expense_input()` had no filter distinguishing a real submitted row from the template's own example rows | Traced why `cm2.total_expense` (Rs47.65L, 0.1% of NSV) looked implausible | Filter on `EXAMPLE ROW` marker in `Remarks` (`scripts/build_dashboard_data.py:load_pl_expense_input()`) | `tests/test_pl_expense_input_filter.py` | HIGH (financial KPI shown as near-100% margin) | RESOLVED, control live in production `data.js` since 2026-09-13 |
| FM-02 | DATA_QUALITY | Two Aug'26 Primary files disagreed by ~Rs2.14 Cr; the gap traced to inconsistent returns/MRN netting between the two | One candidate source (`Primary_Aug26_FY27.csv`) was gross-of-returns; the chosen source (`Aug26_primary_detailed.csv`) nets returns via its own `MTD-Sale type` field, matching the pattern every other monthly file already uses | Row-level channel-by-channel reconciliation (EB2B/SIS matched exactly once returns were excluded from the comparison; the gap was isolated to the returns treatment) | Recommendation registered in `docs/DATA_LINEAGE.md`; every `Primary_Article_Monthly/*.csv` already carries all `MTD-Sale type` values (Sales/MRN/Cancel Invoice/Tester-FOC) so the pipeline nets by summing signed values, not by filtering rows -- this convention should be checked, not reinvented, for every future month | **NOT YET BUILT**: no automated month-over-month returns-rate control exists; would need a script comparing `MRN` value as % of `Sales` value per month, flagging a sudden change | MEDIUM | RESOLVED for Aug'26; pattern documented, generic control not yet built |
| FM-03 | BUG_CODE | A rebuild mode silently dropped `detail_records` coverage from 100% to 95.6% | `--detail-max-rows` CLI default (40,000) does not match the value every actual production build has used (uncapped, `0`) -- an easy mistake for a plain `--detail-only` run to make | Compared `detail_meta.rows_kept`/`rows_total_groups` before and after a rebuild, rather than checking only the aggregate NSV total (which still reconciled even with the cap applied) | `scripts/ci_validate_datajs.py` now prints coverage and WARNs below 99% | none yet -- the WARN is post-hoc (after a build), not pre-commit; **NOT YET BUILT**: a pre-build reminder or CLI default change | HIGH (silently degrades every FY's drill-down granularity, not just the new month) | RESOLVED for the Aug'26 build; systemic fix (change the CLI default to `0`, or hard-fail on a coverage drop rather than WARN) recommended in `docs/WORKFLOW_AND_MODEL_AUDIT.md` Section 5, not yet implemented |
| FM-04 | BUG_MAPPING | A source file's own `Direct/Distributor` column read "Direct" for 100% of its 16,488 rows regardless of actual billing type | Found while reconciling the two Aug'26 Primary candidates -- the superseded file's tagging field cannot be trusted | Cross-checked against the chosen source's equivalent field (`PO Type`), which correctly shows both `Direct` and `Dist.` billing within the same chain | The superseded file is no longer used for the Aug'26 total; caveat recorded in `docs/DATA_LINEAGE.md` | none (the affected file itself is superseded, not fixed) | LOW (does not affect production since the file isn't used for this purpose) | RESOLVED by not using the affected column, not by correcting it |
| FM-05 | BUG_METADATA -- **RESOLVED 2026-09-22** | `dashboard/data.js` carried a duplicate `meta`/`metadata` block with stale, mislabeled FY-month content | Two competing generators exist: `build_dashboard_data.py` (production) and `scripts/sync_data_js.py` (legacy "Tier 3" pipeline, reading `data_master.json`) both capable of writing `data.js`'s top-level shape -- but confirmed by direct `grep` that `build_dashboard_data.py` **never writes** a `"metadata"` key at all, only `sync_data_js.py` does (line 197). The stray key currently in the committed file was a leftover from some earlier `sync_data_js.py` run (or manual merge); every subsequent `build_dashboard_data.py` partial-refresh mode loads the existing `data.js`, mutates only specific keys, and writes the rest back unchanged -- so nothing ever pruned it, and it would have persisted forever | Traced `scripts/sync_data_js.py:197` (`"metadata": meta,`) after a first documentation pass incorrectly called the block "unreachable" -- corrected on a deeper search; confirmed `index.html` only ever reads `meta`, never `metadata` (zero rendering impact, zero financial-truth impact) | **Fixed at the single common write choke point**: `_safe_write_data_js()` (`scripts/build_dashboard_data.py`) now parses the payload, strips a top-level `"metadata"` key if present, and re-serializes before writing -- applies to every build path (full build and all partial-refresh CLI modes) since they all funnel through this one function. Rebuilt the committed `data.js` via `--detail-only --detail-max-rows 0`: diffed before/after and confirmed `metadata` is the **only** key removed, every other key byte-identical | `tests/test_fm05_metadata_stripped.py` (5 tests: a stray `metadata` key is stripped on write; a payload without one is unaffected/byte-identical; a malformed or NaN payload fails closed -- refused with `ValueError` before any disk I/O, the existing `data.js` left byte-identical and no temp file left (owner decision 2026-09-26, #158; this replaced an earlier "written unchanged" expectation); the currently-committed production `data.js` itself now has no `metadata` key) | MEDIUM (no live financial-truth impact ever -- `index.html` never read this block -- but a standing invitation for a future session to mistake it for authoritative, and an undefined `WARN` left as permanent background noise) | **RESOLVED** -- `scripts/ci_validate_datajs.py` no longer WARNs (verified: `OK baseline invariants hold (7 checked)`, no WARN line at all); control is structural (the write choke point), not a one-time manual edit, so this cannot silently reappear from any future build path |
| FM-06 | SOURCE_CONFLICT | Two files claimed to represent the same Aug'26 Primary period and disagreed | Different extraction grains (invoice-line vs. customer-aggregate) and different returns treatment, not a single simple cause | Full row-level reconciliation (see FM-02); classified `SUPERSEDED` for the losing file, not `CONFLICTING` left unresolved | Recommendation + reconciliation registered in `docs/DATA_LINEAGE.md`, with the authoritative source now in production | **NOT YET BUILT**: no automated "does another file claim this same dataset+period" scan runs before ingestion -- this was found by manual investigation, not a tool | HIGH while unresolved, RESOLVED now | RESOLVED for this instance; generic source-conflict pre-flight check not yet built |
| FM-07 | DOCUMENTATION_ERROR | A registry doc wrongly reported FY25 Primary/Offtake as "AVAILABLE" when neither exists in this repo | The entry was written from an earlier, less-targeted search and never re-verified against the live `data.js` structure directly | Queried `dashboard/data.js`'s actual `primary.fy_tags` / `offtake.*` keys directly rather than trusting the doc's own prior claim | Corrected `docs/DATA_AVAILABILITY_MATRIX.md` with the verified evidence; this failure mode (documentation drifting from the artifact it describes) is exactly why this repo's own rule is "verify against the live file, don't trust a prior doc claim at face value" | none automated -- **NOT YET BUILT**: a script diffing registry claims against `data.js`'s actual keys would catch this class of drift automatically | HIGH (a wrong "available" claim could cause a future agent to build on a foundation that isn't there) | RESOLVED for this instance; class of error (doc vs. artifact drift) remains a standing risk without an automated check |
| FM-08 | BUSINESS_RULE_UNCLEAR | A real "Freight" claim type in a newly supplied Finance file was at risk of being conflated with BL-16's "Logistics Cost" methodology | Same English word, completely different business concept (ad hoc per-transaction distributor reimbursement claims vs. a comprehensive rate-card % of NSV) | Compared magnitudes (~140x difference) and structure before accepting any equivalence | Explicitly registered the non-equivalence in `docs/BUSINESS_LOGIC_REGISTRY.md` (BL-16) and `config/data_source_registry.yml`; **the general control is a checklist item, not a script**: never equate two data fields because they share a name or a keyword -- verify magnitude and structure first | none automated | MEDIUM (a wrong equivalence would materially understate a real cost) | RESOLVED for this instance; the general discipline is documented, not tool-enforced |
| FM-09 | SOURCE_GAP misclassified | Aug'25 Offtake and Aug'26 chain-level Primary were, in earlier sessions, wrongly reported as "missing" when they existed elsewhere in the data (a different key/block, or requiring a governed allocation tool instead of a naive groupby) | "Not in the raw watch-folder I checked" was treated as "does not exist," without checking `config/data_source_registry.yml` / `scripts/data_catalog.py` first | User pushback forced a deeper, targeted search each time | `CLAUDE.md`'s THE ONE FY RULE section now states explicitly: "Never report a month unavailable without checking the registry and matrix first" | none automated -- this is a process discipline, enforced by instruction, not by code | HIGH (repeatedly wastes a session re-deriving something already known) | RESOLVED as a standing rule in `CLAUDE.md`; no code-level enforcement exists (nor obviously could) |
| FM-10 | TECHNICAL_DEBT | Two independent pipelines (`build_dashboard_data.py` and `scripts/sync_data_js.py`) are both structurally capable of writing `dashboard/data.js` | Historical: `sync_data_js.py`'s "Tier 3" consolidation effort was superseded by the current single-generator architecture, but the old scripts, `data_master.json`, and its CI workflow were never deleted | Found while investigating FM-05 | `.github/workflows/validate-data.yml` is `workflow_dispatch`-only; `scripts/ci_validate_datajs.py` WARNs on divergence | none dedicated | MEDIUM | CLOSED_PROVISIONAL -- see FM-05; full retirement (delete the legacy scripts/workflow/data_master.json) requires explicit authorization per this project's non-destructive-ingestion rule, not performed unilaterally |
| FM-11 | DOCUMENTATION_ERROR / SOURCE_GAP misclassified | A properly-governed BL-16 CM2 decision register (D1-D11, all correctly `PENDING_APPROVAL`) was built on 2026-07-24 but never merged to `main`; a later, less rigorous session then self-certified the same real rate card as "Approved" and that IS what reached `main` | Two parallel development efforts on the same real business input, only the weaker one got merged; nobody compared the two before this pass | `git log --all -S"FY24-25"` / commit-message search surfaced the unmerged commit; cross-checked its numbers against the merged version's (identical, confirming same real rate card, not a fabrication) | Recovered the better artifact to `PowerBI/Reference/CM2_Provisional/recovered_governance_20260724/` with full provenance; BL-16 updated | none automated -- **NOT YET BUILT**: no check compares an unmerged branch's governance artifacts against what actually reached main before a feature branch is abandoned | MEDIUM (a better decision framework sat unused for ~7 weeks) | RESOLVED for this instance (recovered, documented); the general pattern (unmerged branches carrying better governance than what shipped) has no automated detector |
| FM-12 | SOURCE_GAP misclassified (self-correcting) | A real, finer-grain version of an already-registered source (FY25 Distributor Secondary at chain+brand grain, not just national total) sat unused in the repo the entire time, never flagged as available at that grain | Nobody had checked whether a file already in the repo, under a name that reads as "Primary" (`DistPrimary_Sheet1_FY24-25.csv`), actually ties to the already-known Secondary total until this pass's Git archaeology | Row-count/value reconciliation against the registered national total (99.97% match) | Registered in `config/data_source_registry.yml` (`fy25_secondary_chain_brand_detail`); `docs/DATA_AVAILABILITY_MATRIX.md` updated | none automated -- **NOT YET BUILT**: no scan checks whether an existing repo file's aggregate ties to an already-registered source total at a different grain | LOW (no wrong output, just an underused asset) | RESOLVED (found and registered); dashboard wiring is the next bounded step, not yet done |
| FM-13 | BUG_CODE (near-miss, caught before merge) | A synthetic FY25 "Primary" derivation (Pareto-fallback article split of Secondary data, self-documented as a proxy) was briefly wired into the live `dashboard/data.js` by a commit from a different, less careful session ("Claude Haiku 4.5"), then correctly removed | A weaker session treated "I can compute a number" as equivalent to "this is real Primary data" | Confirmed via Git history that the commit enabling it (`bccb974`) is real, and confirmed via the current `data.js` that it is NOT present today (`primary.fy_tags == ['fy26']` only) | Documented in `docs/GIT_RECOVERY_MATRIX.md` candidate #4 explicitly so a future session doesn't "recover" this synthetic data thinking it was lost by accident | none automated -- **NOT YET BUILT**: the synthetic/example-row firewall (FM-01's fix) only covers `PL_Expense_Input.csv`'s marker convention, not this class of "plausible-looking derived number presented as an actual" | HIGH (would have overstated FY25 Primary by Rs233.25 Cr had it stayed) | RESOLVED historically (someone already caught and reverted it); this pass's contribution is making sure it isn't mistakenly un-reverted by a future "data recovery" effort |
| FM-14 | TECHNICAL_DEBT (documented, not built) | Provision, Claims and Trade Spend can describe the same commercial event at different lifecycle stages (accrual/provision -> claim -> settlement); naively summing all three sources would double-count | No incident has actually occurred yet in this project -- flagged proactively because `mt_provision_national_aug26` (Provision) and `cm2_claims`/`ClaimMaster_Quarterly` (Claims) both now have real Aug'26-adjacent data and a future CM2 build could plausibly sum both without checking | None yet (proactive, not incident-driven) | **NOT YET BUILT**: no canonical commercial-event key exists to detect Provision-to-Claim-to-Settlement matches across these sources | none | MEDIUM (real risk to CM2 accuracy once these sources are actually combined) | OPEN -- registered here so a future CM2 integration pass checks lifecycle overlap before aggregating, rather than discovering the double-count after publishing a wrong number |
| FM-15 | SOURCE_CONFLICT (documented pattern, partial tooling) | Two files can represent the same dataset+period (Aug'26 Primary, FM-02/FM-06) without either file declaring itself authoritative or superseded | No machine-readable "this supersedes that" relationship exists between source files -- resolution has depended on manual row-level reconciliation each time | Manual reconciliation, twice now (Aug'26 Primary here; implicitly also candidate #2 in `docs/GIT_RECOVERY_MATRIX.md`) | Recommendation registered in `docs/DATA_LINEAGE.md` each time it occurs | none automated -- **NOT YET BUILT**: a pre-ingestion scan that flags "another file already claims this dataset+period" before a new file is wired in | MEDIUM-HIGH (each occurrence costs a full manual investigation) | OPEN -- the general control (source-conflict pre-flight check) remains the single highest-value piece of automation this register recommends and does not yet have |
| FM-16A | `BUSINESS_ALLOCATION_INPUT_UNAVAILABLE` | `Dist_primary_cont_based_on_secondary_MOM.xlsx` -- a real, business-maintained monthly input owned by the MT Channel Analyst Lead -- is not present in this build environment | Correctly kept outside Git for security (raw commercial contribution data); this is expected, not a defect, on a fresh session | Checked `--src` and the working tree for the filename; absent | n/a -- the file must be supplied via the approved local `--src` directory, never committed | LOW on its own (the fallback below covers most of the gap) | OPEN -- **external, requires the business owner to supply the file into `--src` for the next build** |
| FM-16B | `PATCH_SOURCE_INCORRECTLY_OVERRODE_BASE_SOURCE` -- **FIXED AND VERIFIED 2026-09-13** | `load_dist_cont_weights()` (`scripts/build_dashboard_data.py`) checked `PowerBI/SeedData/DIST/DistPrimaryContWeightsArticle.csv` first and, if it existed AT ALL, never looked at the XLSX -- an exclusive either/or instead of a per-key override. That CSV has held exactly 27 "Approved Patch" exception rows since it was created (`git log --follow`, 3 commits, unchanged row count), never the full monthly split | The loader was written as a single if/else instead of base-plus-overlay | Traced the function line by line; confirmed the CSV's row count never changed in Git history | **Rewrote `load_dist_cont_weights()`**: loads the XLSX as base when present, applies the patch CSV as an override/insert keyed on (Ship To Name, Brand, Month) -- confirmed from the patch rows' own `Basis` column, which documents each as a full nearest-month split copied in for one exact key, never a partial in-key correction. New helper `_load_dist_cont_patch_rows()` validates the patch's schema explicitly (raises `SystemExit` naming FM-16B on a missing column) instead of silently ignoring a malformed file. **A second, independent bug found and fixed in the same trace**: the month-parsing branch used `dtype == 'object'`, which stopped matching text columns under pandas 3.x (a new `str` dtype pandas 2.x never had) -- replaced with `pd.api.types.is_numeric_dtype()` so the branch no longer depends on which pandas version drew that boundary. Verified with 4 synthetic tests (`tests/test_dist_cont_loader_fix.py`) covering base-only, base+patch override, patch-alone-never-masquerades-as-complete, and schema-validation-fails-loudly | `tests/test_dist_cont_loader_fix.py` (4 tests, all passing); real-data before/after in `docs/ALLOCATION_CAPABILITY_MATRIX.md` shows `rows_unmapped` 55,455 -> 167, `chains_allocated_to` 0 -> 29, `unmapped_nsv` Rs183.18 Cr -> Rs0.12 Cr, Primary/Offtake totals and detail coverage unchanged, dashboard sweep 44/44 clean -- **even without the real XLSX supplied yet**, because the fix also correctly restores the Priority-1 ShipTo-primary fallback that FM-16B had been silently blocking | Was P1 (material Distributor Primary unallocated); **downgraded to LOW now the fallback correctly activates** -- FM-16A (the still-missing real workbook) remains the only open item, and is now a coverage-improvement opportunity rather than a P1 correctness defect | **RESOLVED (code)** -- remaining work is FM-16A only (supply the real workbook) plus confirming `mapping_health`'s own completeness % moves once a `--primary-only`/full rebuild is run with this fix (not yet verified -- `mapping_health_block()` isn't recomputed by `--detail-only`) |
| FM-25 | BUG_CODE | P&L tab's TOT%/CM2 KPI cards (NSV (TOT%-scope), TOT Value, Total P&L Expense, CM2 Value, and the "Blended TOT%" text/table row) always showed the FY26+FY27-combined figure regardless of the global FY filter bar -- verified live: selecting FY26-only, FY27-only, or no filter rendered byte-identical HTML for these cards, even though the SAME tab's gross-to-net bridge section a few lines above correctly honors the filter. Live data shows this is not cosmetic: FY26's real blended TOT% is 50.1% and FY27's is 50.2% (different populations, different real answer) -- exactly the "FY27 CM2 / FY26 full-year NSV -- looks perfectly reasonable" risk pattern, currently masked for CM2% only because `has_expense_data=False` (FM-01) makes CM2% a flat 100% regardless of population | `cm2_block()`/`tot_block()` (`scripts/build_dashboard_data.py`) both compute their top-level totals (`total_nsv`/`cm2_value`/`cm2_pct`, `blended_tot_pct`/`total_passon_value`) and by-dimension rollups (`by_chain`/`by_brand`/`by_category`/`by_packsize`) from `base`/`gg = df[fy_ge(df["_FY"], "FY26")]` -- FY26 (full 12mo) + FY27 (partial 5mo) summed together with no per-FY split anywhere except the already-existing `monthly` (per-FY-per-month) array. `buildPnl()` (`dashboard/index.html`) read those combined totals directly and never consulted `fOne('FY')`/`F.FY`, unlike `mappingHealthSection()` (FM-21) and the Primary-Offtake Gap card (FM-22), which already use the by_fy-aware pattern for this exact class of card | Read `cm2_block()`/`tot_block()` end-to-end, confirmed the `fy_ge()`-combined population design by code; confirmed live in the browser (Playwright, `F.FY=[]` vs `['FY26']` vs `['FY27']`, `buildPnl()` re-rendered each time) that the KPI cards' HTML was byte-identical across all three, while `data.js`'s own `cm2.total_nsv` (55,140.26) exactly equals FY26's monthly sum (32,900.37) + FY27's monthly sum (22,239.91), proving the blend | Added `mrp` to `tot_block()`'s `monthly` rows (the one field missing to recompute a precise per-FY blended TOT% client-side from `SUM(passon_value)/SUM(mrp)`; `cm2_block()`'s `monthly` already carried `nsv`/`expense`). In `buildPnl()`, when the global FY filter names a single FY that a block's own `monthly` array actually covers, sum that FY's months client-side for the 5 KPI cards and the "Blended TOT%" text/table row, mirroring the `fOne('FY')`-with-fallback pattern FM-21/FM-22 already established; a disclosure note covers the case where the selected FY isn't in that block's own coverage. The `by_chain`/`by_brand`/`by_category`/`by_packsize` breakdown tables deliberately remain FY26-FY27 combined (already labelled as such in the UI) -- splitting those is a separate, larger change, out of scope for this fix, and is called out explicitly in the tab's own text so it isn't implied to be more FY-aware than it is | `tests/test_pnl_cm2_tot_fy_filter.js` (Playwright, live data): confirms the pre-fix byte-identical behavior would have failed this assertion, confirms FY26/FY27 now render different NSV/TOT% KPI values, and confirms each matches an independent client-side recomputation from `data.js`'s own `monthly` arrays. Full 11-tab x 4-FY-state sweep: 44/44 pass, 0 JS errors. `--detail-only --detail-max-rows 0` rebuild (needed only to add the new `mrp` field): confirmed `detail_meta.rows_kept`/`value_coverage_pct` unchanged at 160,834/100.0% and the ONLY top-level `data.js` block that changed is `tot` (and within it, only the new `mrp` key on each `monthly` row -- `by_chain`/`by_category`/`by_packsize`/`blended_tot_pct`/`total_passon_value` byte-identical before/after) | MEDIUM-HIGH (dormant for CM2% today only because of FM-01's zero-expense gap; live and real for TOT%, a Finance/Tax-reviewed metric, right now -- 50.1% vs 50.2% -- and will become live for CM2% the moment PL_Expense_Input.csv is populated, at which point this would have silently misrepresented a blended two-FY margin as if it were the single FY the user explicitly selected) | RESOLVED (code + regenerated `data.js`), NOT YET MERGED -- branch `fix/pnl-cm2-tot-fy-filter`, draft PR pending human review per this project's merge-approval rule |
| FM-26 | BUG_TEST (false confidence -- unsourced expected value, never asserted) | `tests/smoke_dashboard.js` printed `Offtake baseline: 0 (Expected: 4512)` and still reported PASS. The same unsourced `4512` is `EXPECTED_OFFTAKE_UNITS` in `scripts/verify_dashboard_data.py`, which fails on every run (0 vs 4512, plus a chain check against the 36-chain universe that the legitimately 49-chain data can never meet) and is wired into no workflow. Separately, the smoke test's FY step looked for `<button>FY25</button>` and silently skipped when none existed (the FY filter is a `<select>`), and a Playwright/Chromium revision mismatch made it die before any check unless someone hand-edited an `executablePath`. | (1) 4512 appears in no governed source (`config/baselines.json`, `CLAUDE.md`, `docs/METRIC_REGISTRY.md`); both files arrived in bulk commit `daa6ac8`. (2) The smoke test summed `offtake.by_chain[].total`, a field that does not exist (by_chain rows carry NSV in Rs Lakh as `fy26`/`fy27`/`value`, not units), so the sum is always 0. (3) The value was printed but left out of the pass/fail condition. | Found 2026-09-25 while classifying pre-existing noise during the PR #14 selective recovery. | A test's expected business value must be READ from `config/baselines.json` (the one governed baseline file, also used by `ci_validate_datajs.py`), never typed into the test; anything a test prints as "expected" must be in its pass/fail condition, or labelled informational; a missing UI control is a failure, not a skip. Browser tests launch through `tests/browser_launch.js` (PW_CHROMIUM_PATH > Playwright's bundled browser > PLAYWRIGHT_BROWSERS_PATH/chromium > named error). | `tests/test_smoke_dashboard_governed.js` (tampered baseline copy => smoke exits 1 and names the key; real file untouched; resolver order; missing browser = named error) | MEDIUM (no wrong number reached the dashboard; it hid whether one could) | Smoke test FIXED (PR pending). `scripts/verify_dashboard_data.py` NOT changed: stale, fails loudly, in no CI -- retire or rewrite is an owner decision (MT Analytics). |
| FM-27 | BUG_AGENT_CONFIG (agent skills: silent non-load, stale figures, unsafe auto-fix) | Found 2026-09-26: (a) `.claude/skills/github-qc-before-answer/skill.md` (lower case, no frontmatter) never loaded; its body also listed Reliance Primary FY25 Rs64.43 Cr as available (FY25 has no real Primary source -- `docs/DATA_AVAILABILITY_MATRIX.md`), a stale FY27 YTD figure, and a `c.find('"by_chain"')` snippet that printed the wrong block; (b) 4 project skills had no frontmatter description, so the agent could not tell when to use them; (c) `.agents/skills` (Codex copy) was missing `steward`, `fmcg-decision-leader`, `mapping-approval-governor` and had an outdated `professional-growth-coach`; (d) `dashboard-qa-sentinel` said "Auto-Fix Without Asking": its `auto-fix.js` regex-edited the generated `dashboard/data.js`, then ran `git add/commit/push`; `checkNaN()` returned true when NaN was FOUND (a regression logged as OK); its offtake check expected an object where `offtake.total` is a number; it demanded `Unmapped Chain` be filtered out of chain views; its Rule 5 filled a missing FY with another FY's numbers | No gate ran over agent skills: `validate_skills.py` covers only `skill-suite/`, `sync_skills.py --check` exits 0 for absent/outdated targets and defaults to project-claude, and nothing in CI ran either | Session skill list vs `.claude/skills/` folder; running each skill's snippets and scripts against current `data.js` | Renamed + frontmatter + figures replaced by source pointers (FY25 marked NOT IN REPO); descriptions added; Codex copy synced; sentinel made read-only (reports and routes, exit 1 on critical failure), checks corrected; governed `change-verification` skill added to the suite | `tests/unit/test_skill_layout.py` (5 tests, run by `validate.yml`; 4 of 5 fail on the pre-fix `main`) | HIGH (a loaded skill could quote an unsourced FY25 figure or push a hand-edited `data.js`) | RESOLVED in the skills-hardening PR; open display decision: the Channel & Chain view hides FY27 `Unmapped Chain` (Rs10.13 L of Rs22,239.59 L) -- reported by the sentinel as a non-critical WARN, not changed |
| FM-28 | BUG_CI (false green -- same family as FM-26, in workflow wiring) | Power BI Windows CI (`pbi-windows-ci.yml`) reported success on main while two of its validators exited 1 (run 36217266380, main 69e977c): `scripts/ci/test_powerbi_model.ps1` flagged `00_Parameters.pq` as malformed, and `tests/validate_data_integrity.py` failed 3 assertions. Found 2026-09-26 rebuilding old PR #131 on main 8ba13f8. | (1) `continue-on-error: true` on both validation steps. (2) The harness step ran two python scripts back to back; a pwsh step fails only on its LAST native exit code, so the first script's failure was lost. (3) The M-checker required the text `let`/`in`, but a Power Query parameter (`"..." meta [IsParameterQuery=true, ...]`) is valid M without them. (4) Three integrity checks predated later decisions on main: FY27 read from `primary.by_chain[].fy27` (FY27 Primary is article-level only, `detail_meta.fyx_primary.FY27`); a null FY26 was treated as bad (missing-is-not-zero fix, `test_primary_dim_rows_missing_not_zero.py`); and the `Pan India` offtake zone was banned although it is a real zone and the zones incl. Pan India add up to the offtake total (FY26 31,119.87 = 31,119.87; FY27 19,045.00 vs 19,044.99). | Read the job log of a green run, not just its conclusion; ran each validator and the checker (pwsh 7.4) locally on main: all exit 1. | No `continue-on-error` in the workflow; `$LASTEXITCODE` checked after every python call; `eval_harness.py` (retired `data_master.json` pipeline) is shown as a `::warning::` annotation, not hidden and not blocking; checker accepts parameter queries; integrity checks read the fields main actually uses and still fail on NaN, a missing FY27 total, or a zone double count. | `tests/test_pbi_windows_ci_not_masked.py` (5 of 6 original cases fail on 8ba13f8; 3 mutation cases prove the checks still catch real defects); runs in the required Production Acceptance Gate (`pytest tests/`). | MEDIUM (a real data.js or Power Query defect in this job would have shown green) | RESOLVED on branch `claude/pbi-ci-no-masking`, draft PR, NOT YET MERGED |
| FM-29 | BUG_TEST (environment-coupled harness) | 18 browser tests in `tests/` plus `test_sprint8_compliance_inventory.js` hard-coded `chromium.launch({ executablePath: '/opt/pw-browsers/chromium' })`, and the 18 loaded Playwright from `/home/user/mt-dashboard/node_modules/playwright`. Both paths exist only in the cloud container, so on a runner or local clone the tests die with "Executable doesn't exist" / "Cannot find module" -- an environment failure that reads like a test failure. Found 2026-09-26 by `pr-deep-review` while reviewing #226. | `tests/browser_launch.js` (PW_CHROMIUM_PATH -> bundled -> PLAYWRIGHT_BROWSERS_PATH/chromium) existed but new tests copied the older pattern; CLAUDE.md's validation step named the container path directly. | grep over `tests/*.js` and root `test_*.js`. | All 19 scripts call `launchChromium()`; CLAUDE.md points at the shared launcher. | `tests/test_browser_tests_use_shared_launcher.py` (3 of 4 fail on main 7204491; runs in the required Production Acceptance Gate). All 22 browser scripts give identical exit codes and result lines before and after. | LOW-MEDIUM (false environment failures off the container; no dashboard impact) | RESOLVED on branch `claude/test-browser-launcher-unification`, draft PR, NOT YET MERGED |

## Error-to-pattern routing (the "trace to the earliest wrong layer" rule)

Added 2026-09-13. When a number looks wrong, trace it through the layers below in
order and fix the **earliest** one that's actually broken — never patch a later
layer (a chart, a card, a rendering tweak) to paper over an error that originated
upstream:

```
Source file -> Schema/grain -> Master-data mapping -> Allocation ->
Canonical fact (data.js block) -> Registered measure (docs/METRIC_REGISTRY.md) ->
Filter/comparison logic -> Rendered visual
```

| Symptom | Check first | Matches |
|---|---|---|
| FM-17 | BUG_CODE | `targets`/`insights`/`mapping_health`/`mom`/`scorecard`/`pvm`/`profitability`/`npd`/`readiness` went stale after any partial refresh (`--detail-only` chief among them): "Unmapped Chain" showed as a ~40%-contribution top-2 revenue driver on the Executive Cockpit's "Chain — target vs achievement" table and the Performance & Comparison "Chain scorecard", long after the real Unmapped-Chain bucket had been fixed to <0.1% of Primary everywhere else on the dashboard (`same_period.by_chain`, `fyx_primary.by_chain`, Channel & Chain Performance) | This whole layer was computed ONLY inline in `main()`'s full-build path (lines ~6437-6513 pre-fix); every partial-refresh CLI mode updated `detail_meta`/`primary`/`offtake` but never re-ran it, so it stayed frozen at whatever the last full build had produced -- exactly the risk FM-16B's own closing note flagged for `mapping_health_block()` specifically ("not yet verified — isn't recomputed by `--detail-only`"), generalized here to the whole downstream layer | Live dashboard visual QA (Playwright screenshot review of the Executive Cockpit and Performance & Comparison tabs) surfaced the wrong "Unmapped Chain" figure; traced by comparing `data.targets.measures.*.by_chain` against the already-correct `detail_meta.same_period.by_chain` for the same chain | Extracted the full block into `refresh_derived_blocks(data, src)` (`scripts/build_dashboard_data.py`), called from both the full build and `--detail-only`, so this layer can never drift from what it describes again | `tests/test_refresh_derived_blocks.py` (2 tests: direct-call and full `--detail-only` CLI, both corrupting a real `data.js`'s targets/scorecard first and confirming they come back correct) | HIGH (wrong, leadership-facing "top revenue driver" claim on two dashboard surfaces) | RESOLVED, control live in production `data.js`. **2026-09-22 follow-up**: `--primary-only`, `--offtake-rebuild` and `--offtake-patch` -- the three other modes that actually WRITE `obj["primary"]`/`obj["offtake"]` (the inputs `refresh_derived_blocks()` reads) -- now call it too, closing the exact gap this row flagged as "not yet done". `--forecast-only` and `--universe-only`/`--mapping-health-only`/`--readiness-only`/`--distgap` were checked and deliberately left alone: none of them write to `primary`/`offtake`/`detail_meta`/`alloc`, so calling it there would be a needless no-op, not a fix (`--universe-only`/`--mapping-health-only`/`--readiness-only` already separately recompute the one narrow block + readiness gate they each name). `tests/test_partial_refresh_wiring.py` (structural, 6 tests) proves the wiring is present, in the right order relative to the write, and proves the deliberately-excluded branches genuinely don't touch those inputs -- a full CLI-level run (like `test_detail_only_cli_refreshes_targets_not_just_detail_records` does for `--detail-only`) isn't possible for these three without the real, gitignored source workbooks |
| FM-18 | BUG_CODE (FY05 -- Partial-year mismatch) | Performance & Comparison tab's article-level dimension view (SubCategory/Range/PackSize/Article) showed a direction-reversed, misleading YoY under the default All-FY filter: Face Cleanser rendered **-18.1%** ("decline") when the true like-for-like figure is **+113.1% growth** | `buildComparison()`'s article-level `else` branch (`dashboard/index.html`) computed YoY from full-FY totals (`v[fyA]` vs `v[fyB]`, no month restriction) with no shared-month guard, unlike the Chain/Brand branch in the same function (already period-safe) and unlike `same_period_block()` (`scripts/build_dashboard_data.py`) -- the server-side helper this exact failure mode's own docstring was written to prevent, just never applied to this one client-side path. Comparing full FY26 (12mo) against partial FY27 YTD (5mo, Apr-Aug'26) as if both were full years produces a false-decline or exaggerated/understated figure whenever the two years' run-rates differ across the missing months | Live Playwright verification against production `dashboard/data.js`: replicated the exact client-side calc in `page.evaluate()`, computed both the naive full-FY YoY (-18.1%) and the correct same-period-only YoY (+113.1%) side by side for the same SubCategory, same filter state, proving the direction reversal directly against the rendered DOM (not just the source code) | Extracted the shared-month restriction into `sameFYPeriodGuard(recs, fyA, fyB)` (new page-global function in `dashboard/index.html`, just above `buildComparison()`), mirroring `same_period_block()`'s logic: when the two FYs being compared cover different months, both sides are restricted to the shared months before summing; a no-op (returns the original array unchanged) when both years' month coverage already matches, including the ordinary full-year-vs-full-year case. `buildComparison()`'s article-level branch now calls this instead of aggregating unrestricted. A disclosure note (`articlePartialNote`) renders under the table whenever the restriction is active, so the same-period basis is visible, not silent | `tests/test_fy_same_period_guard.js` (15 tests: a partial-vs-full-year fixture proving the guard both restricts to the correct shared-month count and reverses the sign a naive full-year comparison gets wrong; a full-year-vs-full-year fixture proving the guard is a true no-op, same array reference returned; a no-prior-FY fixture; and 5 live-data tie-back checks against real production `dashboard/data.js` confirming Face Cleanser now renders +113.1%, not the stale -18.1%, with the disclosure note shown) | HIGH (leadership-facing "decline" label on what was actually the channel's strongest same-period growth, on a tab used directly for chain/category performance review) | RESOLVED -- fix in `dashboard/index.html` only (no `data.js`/baseline change); full 11-tab x 4-FY-state sweep 44/44 clean, 0 JS errors; `tests/test_contribution_grouping.js` and `tests/test_npi_filter_aware.js` re-run clean to confirm no adjacent regression from the extraction |
| FM-19 | TECHNICAL_DEBT (dead code, not built) | `PowerBI/SeedData/Masters/PrimaryAllocationOverride.csv` is a real, documented, schema-ready mechanism for business-approved distributor-to-chain overrides (columns: Month, Ship To Name, Chain, Brand, Override Cont%, Remarks; matched on Ship To Name x Brand x Month) with a working loader method, `DistAllocationGovernance.apply_overrides()` (`scripts/dist_allocation_governance.py`) -- but that method is never called anywhere in `scripts/build_dashboard_data.py`'s actual allocation flow. The override CSV's rows are only counted for a governance metric (`alloc["governance"]["override_count"]`), never applied to `_Chain` | `allocate_dist_primary()` builds `_Chain` purely from the cont%-sheet match (exact or nearest-month) and falls straight to `"Unmapped Chain"` on a miss -- `apply_overrides()` exists as a self-contained method but was never wired into that flow, likely built ahead of the loop that would call it and never finished | `grep -rn "apply_overrides(" .` returns only the method's own definition, confirmed while turning 14 business-owner-approved chain-mapping decisions (`config/chain_mapping_policy.yml`, 2026-09-22) into policy -- the approvals have nowhere live to land yet | **NOT YET BUILT**: wire `apply_overrides()` (or an equivalent override-lookup) into `allocate_dist_primary()`, keyed the same way `key_eff`/`key_tier` already resolve cont%-sheet matches, at a precedence ABOVE the exact/nearest cont%-sheet match per the evidence hierarchy `config/chain_mapping_policy.yml` documents; needs the same before/after `rows_unmapped`/`unmapped_nsv` proof FM-16B used, plus a dedicated test | none yet -- blocked on the wiring above | MEDIUM (approved decisions sit inert; NSV is not lost, `Unmapped Chain` total does not shrink even after owner sign-off) | OPEN -- `config/chain_mapping_policy.yml` records the approved decisions so they aren't lost while this gets a bounded, tested fix; do not explode them into `PrimaryAllocationOverride.csv`'s month-grain rows until this is wired in and provably changes `_Chain`, per this register's own "prove it changes the number, don't assume" discipline |
| FM-20 | BUG_CODE (display, not calc) | Chain Mapping Health's "Unmapped exception register" (`mapping_health_block()`, `scripts/build_dashboard_data.py`) shows `cumulative_pct` reading as high as 100.8% partway down a 10-row list, before settling to exactly 100.0% at the last row -- read by a real business reviewer as broken math ("Cumulative is increased, kindly adjust") on 2026-09-22 | `cumulative_pct` is `running_sum / total_of_all_rows * 100`, and several rows near the tail carry small NEGATIVE `nsv` (return/credit rows), sorted last by `sorted(agg.values(), key=lambda d: -d["nsv"])`. A cumulative % of a NET total mathematically MUST end at exactly 100%, but if later rows subtract from that total, an earlier row's cumulative can legitimately read above 100% before the negative tail pulls it back down -- this is correct arithmetic, not a bug in the sum | Verified the exact sequence live: 63.2% -> 79.6% -> 92.1% -> 97.0% -> 100.8% (flat across 4 near-zero rows) -> 100.6% -> 100.0%, matching two small negative-NSV rows (`Sancus Networks`, `SRIJAN ENTERPRISES`) at the tail exactly as the math predicts | **NOT YET BUILT**: add a one-line disclosure to `out["note"]` (or a conditional caveat) in `mapping_health_block()` noting that return/credit rows (negative NSV) can push `cumulative_pct` briefly above 100% before it settles to exactly 100% at the full list -- a text-only fix, no change to the `cumulative_pct` formula itself, which is correct | none yet | LOW (no wrong total, but real confusion cost -- 5 of 10 rows in one real exception-register export carried this same owner remark) | OPEN -- disclosure fix identified, not yet applied; the underlying math should NOT be changed to "cap at 100%" or "exclude negative rows," either of which would hide the real return/credit activity these rows represent |
| FM-21 | BUG_CODE (FY02/FY03 -- Global filter not propagated / precomputed aggregate ignores filter) | Commercial Analytics > Chain Mapping Health card always showed the LATEST FY's completeness/mapped/unmapped figures regardless of the global FY filter bar -- selecting FY26 alone produced byte-identical rendered HTML to no filter at all | `mappingHealthSection()` (`dashboard/index.html`) read `M.by_fy[fys[fys.length-1]]` unconditionally -- the block never referenced `F.FY`/`fOne('FY')` at all, unlike `npiCohortSection()`'s already-correct pattern (default the shown period to the global FY filter when it names a real period in this block's own coverage, else latest) | Live Playwright verification (part of the global FY filter propagation check, master-prompt Stage 3.5): rendered `mappingHealthSection()` with `F.FY=[]` and with `F.FY=['FY26']`, compared output HTML directly -- `identical_html: true`, confirming the filter had zero effect | Applied the exact `npiCohortSection()` pattern: `const globalFy=fOne('FY'); const cur=(globalFy&&fys.includes(globalFy))?globalFy:fys[fys.length-1];` plus a disclosure note when the selected FY isn't in this block's own coverage. The exceptions/proposals tables below the KPIs are explicitly disclosed as NOT FY-broken-out (the underlying `missing_mapping` aggregation discards its own per-row `fy` field during the `(cust_code, ship_to)` groupby -- a separate, deeper data-layer gap, not fixed here) | `tests/test_mapping_health_fy_filter.js` (Playwright, live-data) | MEDIUM (three KPI cards silently frozen at the latest FY; the exception/proposal tables were never FY-scoped in the underlying data to begin with, so no additional loss there) | RESOLVED for the 3 KPI cards (completeness/mapped/unmapped); the deeper "exceptions/proposals carry no FY tag at all" gap is disclosed in the UI, not fixed -- flagged as a follow-up (would require carrying `fy` through `mapping_health_block()`'s aggregation) |
| FM-22 | BUG_CODE (FY02/FY03 -- Global filter not propagated), same class as FM-21 (Chain Mapping Health, tracked on a separate branch) | Channel & Chain Performance > Comparison tab's "Primary-Offtake Gap" card and chart always showed FY27 (whenever FY27 had any rows) regardless of the global FY filter bar -- selecting FY26 still rendered a "Primary-Offtake Gap (FY27)" heading with FY27's numbers | `primaryOfftakeGapSection()` and `renderPogGapCharts()` (`dashboard/index.html`) each independently computed `win=(g.fy27&&g.fy27.by_month.rows.length)?'fy27':'fy26'` with no reference to `F.FY`/`fOne('FY')` -- `D.primary_offtake_gap` carries the exact same two-literal-key shape (`fy26`/`fy27`) as `mapping_health`'s `by_fy`, just not a generic dict, so it was equally susceptible to this pattern and had it independently | Live Playwright verification, same method used for FM-21: rendered `primaryOfftakeGapSection()` with `F.FY=[]`, `F.FY=['FY26']`, `F.FY=['FY27']` and compared output HTML directly -- FY26 selection produced the SAME output as FY27 selection (`fy26_selection_differs_from_fy27_selection: false`) before the fix | Extracted a shared `pogGapWindow(g)` helper both callers now use: honors `fOne('FY')` when it names a covered key (`fy26`/`fy27`), else falls back to the previous FY27-preferred default. Re-verified live post-fix: FY26 selection now shows "FY26" heading and FY26 data (`fy26_selection_differs_from_fy27_selection: true`); no-filter default unchanged (still FY27) | `tests/test_pog_gap_fy_filter.js` (Playwright, live-data) | MEDIUM (a chain-ops card meant for period-specific Primary/Offtake reconciliation silently showed the wrong period's numbers under an explicit FY26 filter) | RESOLVED, both the text/table section and the chart |
| FM-23 | BUG_CODE (FY12 -- Month/FY key mismatch) | `targets_block()` (`scripts/build_dashboard_data.py`) is fed rows from EITHER `load_targets_csv()` (returns `(fy_tag_string, 'Mon-YY' label, value)`, e.g. `("FY27", "Apr-26", ...)`) OR `load_ty_target()` (returns `(date, 'Mon-YY' label, value)`, e.g. `(datetime.date(2026,4,1), "Apr-26", ...)`) -- confirmed by direct call: when fed the date-shaped rows, `fy_target` collapsed from the correct 12000 (Lakh) to 1000 (~92% understated), `fy_tag` rendered as a raw `datetime.date` object instead of a string like `"FY27"`, and a period-to-date actuals comparison with 3 real months of data only ever matched against 1 surviving target month | `targets_block()` did `fy = target_rows[0][0]` then filtered `{lbl: v for tag, lbl, v in target_rows if tag == fy}` -- correct for string tags (every row in one FY shares the same tag) but for 12 distinct `datetime.date` objects, each only equals itself, so this silently kept just the FIRST month and dropped the other 11. `load_ty_target()`'s date shape exists because its OTHER caller, `forecast_block_ty()`, calls `.year`/`.month` directly on that same first element and can't be changed to a tag string without breaking that caller -- a genuine shape conflict between two callers of the same loader, not a simple oversight | Traced while auditing the Demand & S&OP FY contract (continuing the FY-semantic audit past the global filter propagation and precomputed-aggregate checks): read both loaders' actual return shapes, then reproduced the collapse with a direct `bdd.targets_block()` call using synthetic rows matching `load_ty_target()`'s real shape -- `fy_target` came back `1000.0` instead of `12000.0`. Checked the live `dashboard/data.js`: **dormant, not yet triggered** -- its `targets.source` field shows the `load_targets_csv()` fallback was used in every build so far (the real, gitignored `FY2627_TGT_and_sales_team_mapping.xlsb` has apparently never been supplied to a build in this environment) | Normalized at the point of consumption (`targets_block()` itself, not either loader): when the first tuple element has a `.year` attribute (a date), convert every row's tag to its real `fy_tag_from_ym()` string before proceeding -- both shapes now produce identical output for the same underlying calendar data, and `forecast_block_ty()` (unchanged) keeps receiving the raw dates it needs | `tests/test_targets_block_shape_normalization.py` (5 tests: date-shaped rows produce the correct 12-month `fy_target`/string `fy_tag`, correct period-to-date actuals matching across all real months, CSV-shaped rows unaffected, and both shapes proven to produce identical output for the same data) | HIGH (would silently understate the FY target by ~92% and falsely inflate Achievement %/run-rate status to "ahead of target" on the Executive Cockpit and Demand & S&OP Planning tabs, the moment a real target `.xlsb` is next supplied to a build) | RESOLVED in code; dormant in the currently-committed `data.js` since the CSV fallback has always been used so far -- the fix protects the NEXT build that supplies the real xlsb, which would otherwise have silently corrupted it |
| FM-24 | BUG_CODE (mock-data landmine, same class as PR #157's `sync_compliance_data.py` fix) | `.github/workflows/daily-sidecar-refresh.yml`'s scheduled run failed with a Python traceback (`jsonschema.exceptions.ValidationError: 'insights' is a required property`) -- but all 21 of this workflow's scheduled runs since creation had already been failing the same way, every single day, since day one. Investigating why revealed the real risk: `pipeline_generate_sidecars.py` has no real alerts/opportunities loader on ANY code path, and the daily cron invocation never passes `--audits`/`--zones`/`--chains` -- so the schema bug was the ONLY thing stopping this pipeline from silently committing fabricated compliance/enriched numbers, unstamped, over `dashboard/compliance_metrics.json`/`dashboard/enriched_metrics.json` (both fetched and rendered at runtime by `dashboard/index.html`'s Store Audit Scorecard and Supply Chain & Inventory tabs), the moment the schema error was naively "fixed" | `process_generated_insights()`'s output shape (`executive_summary`/`alerts`/`growth_opportunities`, severity `HIGH`/`MEDIUM`) never matched `schemas/generated_insights.schema.json`'s actual contract (top-level `insights` array required; different `alerts` item shape) -- this file isn't fetched/rendered anywhere in `dashboard/index.html` so the mismatch never reached the UI, it only broke this script's own validation every run. Separately (the real finding): no confirmation guard existed on the write path at all, unlike the sibling generator `scripts/sync_compliance_data.py` which PR #157 (2026-09-18, merged 2026-09-22 -- see that PR's own evidence of `compliance_metrics.json` already carrying fabricated data with a template `[85, 84, 86, 85, <current>]` trend fingerprint) had already fixed for a DIFFERENT generator of the SAME two live files | `mcp__github__get_job_logs` on the failed run's job showed the real Python traceback (not just "exit code 1"); `mcp__github__actions_list` (`list_workflow_runs`) showed all 21 runs, going back to run #1, share `conclusion: failure` -- so the automated workflow itself had never actually pushed a commit; `git log --all --follow` on `dashboard/compliance_metrics.json` surfaced the existing, unmerged `6f74a95`/PR #157 fix as a sibling incident of the identical pattern | Reshaped `process_generated_insights()` to match its schema. Added the identical fail-closed guard PR #157 established: requires an explicit `--i-understand-this-is-mock-data` flag to write synthetic values to the live `dashboard/*.json` paths when no real source is given; without it, exits 0 (not an error -- the correct steady state until a real source is wired up) with nothing written. When it does write synthetic data (opt-in only), stamps `is_synthetic=true`/`data_status=SYNTHETIC_DEMO`/`source=...` on every output's metadata, matching PR #157's exact vocabulary so its already-merged provenance banners pick it up correctly regardless of which of the two generators wrote the file | `tests/test_sidecar_pipeline_fails_closed.py` (4 tests: the exact daily-workflow CLI invocation exits 0 and touches no live file; `process_generated_insights()`'s output now validates against its own schema; the opt-in mock path stamps every one of the 3 outputs `is_synthetic`; the confirmation flag is discoverable via `--help`) | HIGH (would have silently reintroduced fabricated, unlabeled leadership-facing audit data the moment the schema bug alone was "fixed") | RESOLVED in code, on its own branch pending merge; PR #157 (the sibling fix for `scripts/sync_compliance_data.py`) is merged to `main` |
| Two totals for the same period disagree | Source-conflict reconciliation (`docs/DATA_LINEAGE.md`) | FM-02, FM-06, FM-15 |
| A number that used to exist now shows blank/zero | `docs/GIT_RECOVERY_MATRIX.md` + `docs/DATA_AVAILABILITY_MATRIX.md` before assuming the source never existed | FM-07, FM-09 |
| CM2 or another expense-derived KPI looks implausible (too high margin, too low cost) | Is the input a real submitted row or a seed-template example row? (`grep -i "EXAMPLE" the source CSV`) | FM-01 |
| A rebuild changes a total that should have been untouched | `detail_meta.value_coverage_pct` before/after; did a `--detail-max-rows` cap change silently? | FM-03 |
| A source's own tag/classification field looks inconsistent with its own totals | Cross-check against a sibling field on the same file, or another file covering the same scope, before trusting either | FM-04 |
| `data.js` has a field nothing in `index.html` reads | Check which generator actually wrote it (`grep` the field name across `scripts/*.py`) before assuming it's dead | FM-05, FM-10 |
| A methodology looks "Approved" but something feels off | Check the approval date against the actual approval-request document's date, and who/what is named as approver | BL-16's own authenticity finding; FM-11 |
| Provision, Claims and Trade Spend sources might overlap for the same cost | Do NOT sum them; check for a shared business key (chain/scheme/period/amount) first | FM-14 |
| A dashboard card shows a number nobody can find the formula for | `docs/METRIC_REGISTRY.md` — if it's not there, it's an unregistered calculation and should be added or reconciled against an existing registered measure | new, 2026-09-13 |
| A YoY/comparison figure looks implausible or opposite-direction when a partial current FY (e.g. FY27 YTD) is involved | Is BOTH sides of the comparison restricted to the same shared months, or is one a full year and the other partial? Check for a `same_period_block()`-style guard (server) or `sameFYPeriodGuard()` (client, `dashboard/index.html`) on that exact code path -- do not assume every comparison path already has one just because `same_period_block()` exists elsewhere | FM-18 |
| A business-owner-approved chain mapping doesn't seem to change the dashboard's Unmapped Chain figures after being recorded | Is `DistAllocationGovernance.apply_overrides()` (`scripts/dist_allocation_governance.py`) actually called from `allocate_dist_primary()`, or just present as an unused method? Check `grep -rn "apply_overrides(" scripts/` -- a hit only inside its own definition means it's dead code | FM-19 |
| A cumulative-% column in a value-ordered exception list reads above 100% partway down, then settles back | Are any rows in that list negative (returns/credits)? A cumulative % of a NET total will always end at exactly 100% but can read >100% at an earlier row when later rows subtract from the total -- mathematically correct, not a bug, but needs a disclosure caveat so a reviewer doesn't read it as broken math | FM-20 |
| A card's numbers don't change when the global FY filter bar is used | Render the section function with `F.FY=[]` vs `F.FY=['FYxx']` and diff the HTML directly (don't just read the source) -- a `D.<block>` read with no `fOne('FY')`/`recFilter()`/`fOn('FY')`/`fHas('FY',...)` anywhere in the function is the signature (check ALL FOUR accessor spellings, not just `F.FY` -- a plain-text grep for `F.FY` alone gives false positives); `npiCohortSection()` is the reference for the correct pattern. A block with only TWO literal per-FY keys (e.g. `g.fy26`/`g.fy27`, not a generic `by_fy` dict) is equally susceptible and easy to miss with a `by_fy` grep alone | FM-21 (Chain Mapping Health, separate branch), FM-22 (this one) |
| Two loader functions feed the same consumer function but were written at different times | Check that every tuple/dict SHAPE they return actually matches, not just the field COUNT -- a function fed `(tag_string, label, value)` by one loader and `(date_object, label, value)` by another will misbehave silently if it relies on tuple-position semantics (e.g. `row[0] == row[0]` comparisons); check every OTHER caller of each loader too before changing either one's shape, since a fix for one consumer can break another | FM-23 |
| A CI job that writes to a live dashboard `.json`/data file has been failing on every scheduled run | Check `mcp__github__actions_list`'s full run history, not just the latest run -- a permanently-red job may mean it has NEVER actually committed anything (good), but also means nobody would notice the moment an unrelated fix makes it start succeeding. Before "fixing" the failure, check whether the script has a real-vs-synthetic-data guard at all, and whether a sibling generator for the same output file has already hit this (`git log --all --follow` on the output file, and check for open, unmerged PRs matching the pattern) | FM-24 (sibling: PR #157's `sync_compliance_data.py` fix) |

This table is deliberately built from FM-NN rows that already happened in this
project, not a hypothetical list — extend it the next time a new class of failure
is found, following the same "what pattern does this match" question.

## What this register is NOT

This is a documentation-level catalogue, not an automated pre-flight validation
system. A 2026-09-13 request asked for a much larger preventive-engineering
program (per-dataset data contracts, monthly source fingerprinting, an automated
source-conflict detector, specialized sub-agent roles for source integrity/
mapping/finance-methodology/semantic-relationships/dashboard-QA/release-gating,
and machine-readable project state beyond what `docs/PROJECT_STATE.md` already
provides). Building all of that in one pass would mean either doing it shallowly
(defeating the point) or claiming controls exist that don't -- neither is
acceptable under this project's no-fabrication rule. What IS built, real, and
running today:

- `config/data_source_registry.yml` + `scripts/data_catalog.py` -- source
  registration and lookup (built in an earlier pass, extended since).
- `scripts/ci_validate_datajs.py` -- the closest thing to a release gate today:
  JSON validity, baseline invariants, detail-coverage regression (FM-03), and
  stale-metadata divergence (FM-05).
- `tests/test_pl_expense_input_filter.py` -- the one dedicated regression test
  born directly from a failure mode (FM-01).
- This register -- the FM catalogue itself.

What is recommended but **not built** this pass, so a future session does not
mistake documentation for implementation: automated schema-drift diffing per
new monthly file, source fingerprinting, a dedicated returns-rate control,
pre-ingestion source-conflict scanning, and the specialized sub-agent roles.
Each is a genuinely separate, scoped implementation task -- listed here so the
next session picks up from an accurate starting point rather than re-deriving
whether they exist.
