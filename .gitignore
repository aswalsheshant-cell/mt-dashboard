# Python
__pycache__/
node_modules/

# Forecast pipeline run artifacts
forecast_outputs/

# Phase A generated data — real business data, local only
Phase_A_Input/*.csv
Phase_A_Input/*.xlsx
# Track schema templates and configuration files (no real business data)
!Phase_A_Input/events_calendar.csv
!Phase_A_Input/launch_plan.csv
!Phase_A_Input/targets.csv
# audit outputs contain no sensitive data; track the JSON/CSV summaries
# (but Excel may have raw rows — keep it local)
audit_output/*.xlsx

# Source data — keep OUT of the repo (large; only generated dashboard/data.js is committed)
*.xlsx
*.xlsb
*.xlsm
*.csv.gz
primary_article.xlsb
# but DO track the small committed seed/template/mapping CSVs under PowerBI/
!PowerBI/**/*.csv
!margin_repository/*.xlsx
# Track the business input approval tracker (no sensitive data — schema/status only)
!BUSINESS_INPUT_APPROVAL_TRACKER.xlsx
