# Python
__pycache__/
node_modules/

# Source data — keep OUT of the repo (large; only generated dashboard/data.js is committed)
*.xlsx
*.xlsb
*.xlsm
*.csv.gz
primary_article.xlsb
# but DO track the small committed seed/template/mapping CSVs under PowerBI/
!PowerBI/**/*.csv

# Phase 3 test output artifacts (timestamped, regenerated per run)
docs/PHASE_3_VALIDATION_*.txt
docs/PHASE_3_SIGNOFF_*.txt
docs/PHASE_3_RESULTS_*.json

# skill-suite: local backups of diverged install targets
work/skill-suite-backups/

# Generated PPTX output — produced by scripts/export_slides.js and build_*_presentation.js
MT_Leadership_Report.pptx
Central_Zone_Leadership_Pack_*.pptx
July_MT_Command_Centre_*.pptx
tpl_cmd/
unpacked_enriched/

# Playwright test artifacts
playwright.config.js
test_sprint6_e2e_local.js
test-results/

# GitHub Pages build output (Jekyll)
_site/

# Backup archives (user-downloaded backups)
mt-dashboard-backup-*.zip

# generated leadership decks (rebuild with scripts/build_mt_july26_audit_deck.py)
MT_July26_Offtake_Audit_Nielsen.pptx

# Raw monthly source drops — 875 MB of store x article extracts, staged for
# scripts/build_dashboard_data.py. Kept on disk, never committed (same rule as
# the PowerBI/RawDataFolders sources above). The small derived aggregate IS
# tracked: the July'26 audit deck cites it for the Jul'25 and FY26 series.
data/raw_drops/*
!data/raw_drops/_agg/
data/raw_drops/_agg/*
!data/raw_drops/_agg/*.json
